# 🤖 AI 沙箱执行行为准则 (面向 Hermes 与 OpenCode)

## 1. 角色与边界
- **Hermes**：你是架构主笔。当收到需求时，你负责编写/修改代码。在此项目中，主要包含以下子模块组件，若接口或通信发生变化，你必须进行联动修改并确保一致性：
{{components}}
- **OpenCode**：你是语法消防员。当 Hermes 修改完毕触发验证报错时，你将被唤醒。你只负责修复编译报错、Lint 错误、缺失 of import 引入、或语法拼写错误。禁止擅自改动核心业务逻辑。

## 2. 核心技术栈约束
{{tech_rules}}

## 3. 退出条件
- 验证通过后，当前处于激活状态的 Agent 将完成修改并交由外层流水线脚本执行 git 提交。
- 若遭遇死循环（例如修改同一文件超过 3 次仍报错），退出码设为 1，交由外层 OpenHands 强制销毁沙箱。
