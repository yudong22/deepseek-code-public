### 🚀 新功能
- feat: 统一按钮样式并移除设置按钮
- feat: add websearch citation prompt to system messages (Claude Code prompt() pattern)
- feat: add current date, search limit, and source REMINDER to websearch
- feat: TodoWrite tool + TodoUpdated event + safety module + primed_by_untrusted guard

### 🐛 Bug 修复
- fix: disable websearch tool registration for DeepSeek
- fix: improve websearch error when provider doesn't support search
- fix: switch websearch to SearXNG public instances (DDG API returns empty)
- fix: convert DDG search to reqwest::blocking + add Cargo feature
- fix: ctx → _ctx since DDG API doesn't need ToolContext
- fix: switch DDG search to Instant Answer API (api.duckduckgo.com)
- fix: remove duplicate fetch_result block in do_ddg_search
- fix: DDG — use html.duckduckgo.com with full browser headers + redirect detection
- fix: DDG parsing — class-agnostic link extraction + browser UA
- fix: disable provider search fallback — DDG Lite only
- fix: REMINDER follows Links without blank line separator
- fix: suppress unused duration_ms warning in DDG search path
- fix: websearch bullet regex — make [.)] optional to match DeepSeek format
- fix: use Tauri opener plugin for external markdown links
- fix: open external markdown links in system browser
- fix: remove unused re_snippet variable in DDG fallback path
- fix: websearch — strip JSON duplication, return Claude Code-style clean text
- fix: websearch — remove raw_text bloat, add source REMINDER, minimal API context
- fix: redesign ConfirmDialog with native-feel React styling + command highlighting
- fix: add bullet-list result parser for DeepSeek search format
- fix: rewrite websearch to use lite.duckduckgo.com for stable HTML parsing
- fix: add PolicyConfirm dialog handler in App.tsx for dangerous command confirmation
- fix: add TodoUpdated to Tauri AgentEvent enum + frontend bridge type

### ♻️ 重构
- refactor: simplify websearch — provider-native search only, no scraping
- refactor: swap websearch priority — DDG Lite primary, provider fallback
- refactor: rewrite websearch to use provider-native search API (Claude Code pattern)

### 📦 其他
- debug: include raw DDG API response when no results found
- debug: include first 300 chars of DDG HTML when no results found
- release: v0.6.0 (feat:3 refactor:1 other:1)