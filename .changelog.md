### 🚀 新功能
- feat(v0.6.1): SubAgent progress feedback via ToolProgress events
- feat(v0.6.1): improve subagent tool description + system prompt guidance
- feat(v0.6.1): SubAgent — typed agent definitions, tool filtering, custom agents
- feat(v0.6.1): SubAgent tool + coding tool improvements + frontend fixes

### 🐛 Bug 修复
- fix(v0.6.1): @ file search broken after chip insertion
- fix(v0.6.1): add ToolProgress frontend handler + ExpandableToolCard progress display
- fix(v0.6.1): run subagent loop on dedicated OS thread
- fix(v0.6.1): switch subagent from reqwest::blocking to async reqwest via Handle::block_on
- fix(v0.6.1): custom agents dir .claude/agents → .deepseek-code/agents
- fix(v0.6.1): rewrite SubAgent as internal agent loop, not external CLI

### 📝 文档
- docs: update README, route-map, CLAUDE.md for v0.6.0 — 10 tools, 306 tests, new components

### 🎨 样式
- style: model selector text — label-secondary semantic token instead of hardcoded #333/#d0d0d0
- style: send button lighter — deepseek-600 (#4868b2) / dark: deepseek-200
- style: send button — deepseek-800 (#34415b dark blue-gray) / dark: deepseek-200
- style: send button — bg-label-primary (near-black/white) instead of brand-blue
- style: line numbers — padding-left 2rem, width 2.5rem
- style: line number gutter — right-padding 1.25rem, no left padding
- style: line numbers — shift left, more gap from code
- style: line numbers — smaller (10px), wider (2.25rem for 3-digit), lighter color
- style: increase right-panel code padding-right 1rem→1.5rem
- style: reduce right panel code line-number gutter width 2.5rem→2rem
- style(v0.6.1): unify file chip colors with semantic tokens
- style(v0.6.1): unify ChatInputCard buttons with semantic color tokens