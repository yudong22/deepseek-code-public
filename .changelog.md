### 🚀 新功能
- feat: 生成 changelog 摘要并加入提交信息
- feat: 读工具并行执行 — grep/glob/file_read 通过 spawn_blocking + join_all 并行运行，写入/问答工具保持串行
- feat: max_tokens 自动续写 — finish_reason="length" 时自动注入 "请继续" 消息，不消耗 step 预算

### 📦 其他
- v0.5.0: Rust sidecar-agent 重构

---

## v0.5.1 ✅ 已完成

### ✅ 已完成
1. **读工具并行执行** — grep/glob/file_read 等只读工具通过 `tokio::task::spawn_blocking` + `futures::future::join_all` 并行执行，Tool trait 新增 `is_read_only()` 方法，ToolRegistry 改用 Arc<dyn Tool> 支持跨线程共享
2. **max_tokens 自动续写** — SseChunk 新增 `FinishReason` 变体；agent 检测 finish_reason="length" 时自动注入 `"请继续（不要重复已有的内容）"` 消息，不消耗 25 步预算（上限 5 次续写）

### ❌ 暂不纳入
- 模型 fallback（留待后续版本评估）
